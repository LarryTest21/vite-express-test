import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vite.vue.typescript.starter',
  appName: 'vite-vue-typescript-starter',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
